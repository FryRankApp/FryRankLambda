package com.fryrank.model.enums;

/**
 * Whether the client intends to add (turn on) or remove (turn off) the given {@link ReactionType} for the viewer.
 */
public enum ReactionAction {
    ADD,
    REMOVE
}
